import * as React from "react";

const NewUser: React.FC = (props: NewUserProps) => {
  
    return (
      <h1>New User Info</h1>
    );
  };
  
  interface NewUserProps {}
  
  export default NewUser;